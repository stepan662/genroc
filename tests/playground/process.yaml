# yaml-language-server: $schema=http://localhost:8080/process-schema.json
name: order-pipeline
tasks:
  - id: child
    action:
      type: child_map
      children:
        a:
          name: child
    switch: end