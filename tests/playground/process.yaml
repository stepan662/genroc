# yaml-language-server: $schema=http://localhost:8448/process-schema.json
name: order-pipeline
tasks:
  - id: child
    action:
      type: child
      name: child
      result_schema:
        type: object
        properties:
          test:
            type: boolean
    output: "$: 10"
    switch:
      - panic:
          code: test
          message: test
    on_error:
      - code: ["fourth_%"]
        goto: end
  - id: call
    action:
      type: fetch
      url: ""
      headers:
        test: "100"
    switch: end
output: "$: error"