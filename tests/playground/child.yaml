# yaml-language-server: $schema=http://localhost:8080/process-schema.json
name: child
tasks:
  - id: fourth
    output:
      test: "{{ true }}"
    switch:
      - case: "true"
        raise:
          code: "fourth.failed"
          message: "Fourth task failed"
      - goto: end
output: "{{ outputs.fourth }}"